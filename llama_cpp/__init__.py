class Llama:
    def __init__(self, *a, **k):
        raise RuntimeError("llama-cpp-python not available")
    def __call__(self, *a, **k):
        raise RuntimeError("llama-cpp-python not available")
